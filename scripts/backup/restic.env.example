# restic オフサイト暗号化バックアップ設定（例）
# このファイルを scripts/backup/restic.env にコピーして値を埋めてください

# どちらかを使用: Cloudflare R2 (S3互換) または Backblaze B2 (ネイティブ)

# --- Cloudflare R2（S3互換）の例 ---
# RESTIC_REPOSITORY="s3:https://<accountid>.r2.cloudflarestorage.com/<bucket>"
# AWS_ACCESS_KEY_ID="<R2_ACCESS_KEY_ID>"
# AWS_SECRET_ACCESS_KEY="<R2_SECRET_KEY>"
# AWS_DEFAULT_REGION="auto"
# AWS_S3_FORCE_PATH_STYLE="true"

# --- Backblaze B2（ネイティブ）の例 ---
# RESTIC_REPOSITORY="b2:<bucket>"
# B2_ACCOUNT_ID="<B2_KEY_ID>"
# B2_ACCOUNT_KEY="<B2_APP_KEY>"

# restic パスフレーズ（必須）
RESTIC_PASSWORD="change_this_to_strong_passphrase"

# 保持ポリシー（お好みで調整）
RESTIC_KEEP_DAILY=14
RESTIC_KEEP_WEEKLY=8
RESTIC_KEEP_MONTHLY=6

# バックアップ対象パス（スペース区切り）
BACKUP_PATHS=(
  "$HOME/ai-driven/ai-systems-workspace"
  "$HOME/Library/Mobile Documents/com~apple~CloudDocs/Obsidian"
  "$HOME/whisperx_outputs"
  "$HOME/Backups/db"   # DBダンプ保存先（任意）
)

# 除外パターン
EXCLUDES=(
  "**/.venv" "**/__pycache__" "**/.git" "**/node_modules" "**/*.log"
)


